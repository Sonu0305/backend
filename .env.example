# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/forgot_password_db

# Email Configuration - Resend (HTTP API - Works on Render free tier)
RESEND_API_KEY=re_your_api_key_here
SMTP_FROM_EMAIL=your-email@example.com
SMTP_FROM_NAME=Password Reset Service

# Legacy SMTP Configuration (Won't work on Render free tier - SMTP blocked)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# Application Configuration
FRONTEND_URL=http://localhost:5173
SECRET_KEY=your-secret-key-change-this-in-production
TOKEN_EXPIRATION_MINUTES=30

# Environment
ENVIRONMENT=development
